spring:
  application:
    name: availability-management-service

  mongodb:
    uri: mongodb://localhost:27017/availability_db

server:
  port: 8080
  servlet:
    context-path: /api

logging:
  level:
    org.springframework.data.mongodb.core.MongoTemplate: DEBUG
